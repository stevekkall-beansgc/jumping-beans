# Jumping Beans partner — Cloudflare Pages.
# The _headers file in this dir already sends COOP/COEP/CORP + Origin-Trial.
# CF Pages serves this directory directly, so the build output dir is ".".
#
# Deploy (from the repo root):
#   npx wrangler pages deploy partners/<id> --project-name <id>
name = "petsupply"
pages_build_output_dir = "."
compatibility_date = "2025-01-01"
